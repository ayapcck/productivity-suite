import React from 'react';

import { TodoButton } from '../todoButton';

export default {
    title: 'TodoButton',
    component: TodoButton,
};

export const Basic = () => (
    <TodoButton text='Testing' />
);