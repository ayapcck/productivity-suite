import React from 'react';

import FormButton from  '../button';

export default {
    title: `Form Elements/Button`,
    component: FormButton,
};

export const Basic = () => (
    <FormButton text="Test Button" />
);