import React from 'react';

import { BuildButton } from '../buildButton';

export default {
    title: 'Budget Helper/BuildButton',
    component: BuildButton
}

export const basic = () => (
    <BuildButton />
);